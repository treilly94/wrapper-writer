{%- set name = method.method_name -%}
{%- set params = method.method_params -%}
def {{ name }}({% for p in params %}{{ p[0] }}{% endfor %}):
    api = jvm.TestFunc

    DataFrame(api.{{ name }}({% for p in params %}{{ p[0] }}{% endfor %}), sql_ctx)